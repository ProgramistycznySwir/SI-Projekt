<Page x:Class="SingleNeuronVisualisation.MVVM.View.Points"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SingleNeuronVisualisation.MVVM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Points">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Grid>               
                <TextBlock FontSize="320">
                    ssfsdr<LineBreak />
                    sdrs
                </TextBlock>

                <TextBlock TextWrapping="Wrap" Height="164">
                    <Run Text="Scrolling is enabled when it is necessary. Resize the window, making it larger and smaller."/>
                </TextBlock>
                <Rectangle Fill="Red" Width="500" Height="500" Margin="491,335,-208,-50"/>
            </Grid>
        </ScrollViewer>

        <ListView Grid.Column="1" 
                  HorizontalAlignment="Right" 
                  VerticalAlignment="Stretch" Width="250">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
